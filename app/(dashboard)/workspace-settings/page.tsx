import { WorkspaceSettingsPage } from "@/components/pages/workspace-settings-page"

export const metadata = { title: "Workspace Settings — Postly" }

export default function WorkspaceSettings() {
  return <WorkspaceSettingsPage />
}
