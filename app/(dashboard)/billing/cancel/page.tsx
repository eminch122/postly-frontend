import { CheckoutCancelPage } from "@/components/pages/checkout-cancel-page"

export const metadata = { title: "Payment cancelled — Postly" }

export default function Cancel() {
  return <CheckoutCancelPage />
}
